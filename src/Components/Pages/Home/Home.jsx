import React from 'react';
import CounterContext from '../Context/CounterContext';

const Home = () => {
    return (
        <CounterContext>

        </CounterContext>
    );
};

export default Home;